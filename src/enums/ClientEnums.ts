export enum BrowserOrientation {
  landscape = 'landscape',
  portrait = 'portrait',
}

export enum BrowserSize {
  xs = 'extra-small',
  sm = 'small',
  md = 'medium',
  lg = 'large',
  xl = 'extra-large',
  fs = 'full-size',
}
