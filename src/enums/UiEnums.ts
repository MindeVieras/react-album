export enum UiBrowserOrientation {
  landscape = 'landscape',
  portrait = 'portrait',
}

export enum UiBrowserSize {
  xs = 'extra-small',
  sm = 'small',
  md = 'medium',
  lg = 'large',
  xl = 'extra-large',
  fs = 'full-size',
}

export enum UiSizes {
  headerHeight = 60,
}
